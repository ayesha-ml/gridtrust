# gridtrust
Uncertainty-aware electricity demand forecasting with LightGBM + conformal prediction. Monitors coverage and feature drift in PostgreSQL, auto-recalibrates on reliability breaches, and uses a guardrailed LLM to narrate validated model-health metrics.
